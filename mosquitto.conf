# Mosquitto MQTT broker — minimal development configuration.
# Authentication is intentionally open for local dev; add ACL / passwords for staging+.

listener 1883
allow_anonymous true

# Persist messages across broker restarts (optional for dev)
persistence false

# Log all messages at info level
log_type all
