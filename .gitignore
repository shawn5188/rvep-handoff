# ── Dependencies ──────────────────────────────────────────────
node_modules/
.pnpm-store/

# ── Secrets ───────────────────────────────────────────────────
.env
.env.local
.env.*.local
.env.production
*.pem
*.key
!apps/edge-publisher-go/configs/*.yaml.example

# ── Next.js build artifacts ──────────────────────────────────
.next/
out/
.turbo/

# ── Go build artifacts (edge-publisher-go) ───────────────────
apps/edge-publisher-go/bin/
apps/edge-publisher-go/dist/
apps/edge-publisher-go/*.exe

# ── Python cache (r2-bridge) ─────────────────────────────────
__pycache__/
*.pyc
*.pyo

# ── TypeScript / build outputs ───────────────────────────────
dist/
build/
*.tsbuildinfo

# ── Prisma generated client (regenerated per machine) ────────
apps/backend/prisma/generated/

# ── Test / coverage outputs ──────────────────────────────────
coverage/
.nyc_output/
*.lcov

# ── Dataset recordings (large, machine-specific) ─────────────
/var/lib/rvep/datasets/
datasets/
# ...but the admin UI page named "datasets" is source code, not recordings (c15 P2 fix)
!apps/web/src/app/admin/datasets/

# ── OS / IDE ─────────────────────────────────────────────────
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*.swo

# ── Logs ─────────────────────────────────────────────────────
*.log
npm-debug.log*
pnpm-debug.log*

# ── Playwright / screenshots (dev only) ──────────────────────
.playwright-mcp/
playwright-report/
test-results/

# ── tmp ───────────────────────────────────────────────────────
.tmp-*
*.tmp

# === ⛔ 內部資料 — 絕對禁止 commit 到 public repo（2026-06-05 Shawn 拍板） ===
# 這些是科福內部 know-how / 客戶簽核紀錄 / 踩坑經驗，
# 一旦上 public 即無法收回。任何時候在這個目錄做 `git add .` 都要先檢查。
openspec/
memory/
.claude/
*黃祥*
*-handoff-notes*
phase-*-completion.md
HANDOFF.md
CLAUDE.md
AGENTS.md
CODEX_TASK_OPEN_SPEC_V2.md
ROADMAP.md
